# Configuración del entorno (copiar a .env)
# Aquí SOLO lo específico del despliegue. La configuración de la app (contraseña
# maestra, noAuthentication, youtubeApiKey en F3) vive en server/.config.json.

# Puerto HTTP del servidor (sirve la web + la API)
PORT=3000